[project]
name = "calendar"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pyside6>=6.7",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "ruff>=0.6",
]

[tool.ruff]
target-version = "py312"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
